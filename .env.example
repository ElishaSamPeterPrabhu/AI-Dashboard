# Optional: absolute API origin (default: same origin + Vite /api proxy)
# VITE_API_BASE=http://localhost:3000

# Set to "true" to call POST /api/agents/:agentId/runs (Nest BFF mock or future Trimble proxy).
# AI nodes must have agentId set in the config panel.
# VITE_LIVE_AGENTS=true
